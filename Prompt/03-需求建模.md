# Prompt-one:用于需求建模的要求   
1.功能分解与用例详述的目的、内容和格式是什么？
2.动态行为建模-活动图有哪些组件，有哪些组成？画的时候要注意什么？如何画好？
3.系统级用例图是啥？画的时候要注意什么？有什么组成？


# Prompt-two:功能分解与用例详述
基于我们之前整理的“物联网设备知识图谱交互管理平台”的业务用例表，请完成以下任务：

1. **输出功能清单**：
   按“运维工程师”和“运营经理”两个主要参与者，将业务用例拆解为“一级功能”和“二级功能”。
   - 示例格式：一级功能[设备接入] -> 二级功能[证书校验、初始节点创建]。

2. **撰写核心用例详述（Use Case Specification）**：
   请从业务用例中挑选出最能体现系统特性的 3 个核心用例：
   - **UC-02 构建拓扑关系**（体现知识图谱写入）
   - **UC-04 智能查询图谱**（体现LLM与Neo4j交互）
   - **UC-07 退役报废设备**（体现图谱关联清理与安全）
   
   **输出要求**：
   - 包含：前置条件、主成功场景（Main Success Scenario，步骤需在7步以内）、扩展场景（Extension，异常处理）。
   - **特别注意**：在步骤中要明确提及“系统更新知识图谱节点”、“系统执行Cypher查询”或“大模型解析指令”等系统动作。

# Prompt-three:动态行为建模（活动图）
动态行为建模（活动图）
请根据刚才生成的“用例详述”，，为这 3 个核心用例（UC-02, UC-04, UC-07）分别绘制 **PlantUML 活动图**。
注意活动图的绘制规则、注意事项
**绘图要求**：
1. **使用泳道（Swimlanes）**：
   - 必须包含三个泳道：**用户**（工程师/经理）、**IoT交互平台**（业务逻辑层）、**知识图谱引擎**（Neo4j/数据层）。
   
2. **体现关键逻辑**：
   - **UC-02**：要体现从MQTT消息解析出拓扑信息，并在图谱引擎中“创建节点(Node)”和“建立关系(Relationship)”的过程。
   - **UC-04**：要体现“用户输入自然语言” -> “大模型转译为Cypher语句” -> “图谱执行查询” -> “返回结果”的闭环。
   - **UC-07**：要体现先擦除数据，最后“断开/删除图谱关系”的逻辑。

3. **补充业务规则**：
   - 在关键步骤旁使用 PlantUML 的 `note right` 语法添加业务规则备注（例如：必须双向认证通过、必须级联删除等）。

# Prompt-four:系统级用例图
请结合阶段1的业务用例图和阶段2的功能清单，生成最终的 **系统级用例图 (System Use Case Diagram)** 的 PlantUML 代码。

**要求**：
1. **添加系统边界框**：命名为“物联网设备知识图谱交互管理平台”。
2. **规范命名**：
   - 所有用例名称前加上编号（如 UC01_注册接入）。
   - 包含之前分析出的 <<include>> (包含) 和 <<extend>> (扩展) 关系。
3. **保持一致性**：
   - 参与者保持为：运维工程师、运营经理、IoT设备。
   - 确保包含“阶段1”中提到的所有8个核心用例。
5. **要严格遵循画图的规范**
6. **配色和先前的一致或者不用配色让渲染器选，我希望生成的是Rose配色**