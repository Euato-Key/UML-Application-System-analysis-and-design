我现在需要搞面向对象三的实验，我现在第一步需要搞一个物联网设备知识图谱交互管理平台的访谈录音，要详细的，有用的，详尽的录音，以小吴和某经理的展开。或者有其他的人员一起参与也可以
你能模仿一份给我嘛？
这是基于实验一和实验二的

---
一、实验目的
1．在实验一和实验二的基础上，进一步了解系统分析与设计的迭代过程；
2．加深对类模型、状态模型和交互模型的理解；
3．理解包图与组件图的基本概念；
4．掌握在Rational Rose、Enterprise Architect等工具中绘制对象图和包；
5．探索运用知识图谱等技术加强对智能人机交互技术的运用与体验；
6．借助轻量级知识图谱自动生成“需求-用例-类-代码-测试” 五级追踪链，快捷定位、评估任一变更的影响范围。
7．形成系统的完整的需求分析与设计及报告。
二、实验器材
1．计算机一台；
2．Rational Rose或其它UML工具软件。
3．Kimi-128k，中文输出 PlantUML， https://kimi.moonshot.cn
4．通义灵码（可选），自动生成代码，VS Code 插件市场
5．VS Code ≥1.94，IDE写文档，https://code.visualstudio.com 
6．PlantUML插件，侧边实时看图，零配置，VS Code 插件市场
7．Graphviz Preview（可选），PlantUML自动布局，同上，市场一键装
8．Neo4j AuraDB，知识图谱托管，https://console.neo4j.io

三、实验内容
运用大模型或智能体和知识图谱，进行业务建模（业务获取、业务分析）、需求建模（用户需求、系统功能确定、应用特性约定）和软件系统架构设计、构件定义等开发工作。完成文档和代码的生成、转换，同时生成各阶段工作对象与工作成果制品的管理与变更跟踪所需的知识图谱。

四、评价标准（100 分）
维度	分值	说明
业务用例分析	25	参与者齐全、命名规范
需求追踪链	20	Neo4j 里“UC→Class→Code” 三级完整
首版类图/序列图	15	与代码保持一致
变更影响报告	20	能高亮≥5 个受影响节点
文档规范	10	模板统一、走查清单签字
团队互评	10	现场互评问卷

五、常见问题（FAQ）
Q1 没有显卡怎么办？
· 用 Kimi-128k 在线版，PlantUML 生成后拷回本地。
Q2 LLM 乱造属性？
· 先把“业务术语表”贴回 Prompt，再让它“只使用表中词汇”。
Q3 Neo4j 端口冲突？
· 改 conf/neo4j.conf  dbms.connector.bolt.listen_address=:7688
六、示范项目报告（精简版）
· demo_hospital/
├─ 1_biz/          → 访谈录音+用例图
├─ 2_req/          → 功能清单+UC 详述
├─ 3_design/       → 类图+包图+序列图
├─ 4_code/         → Spring-Boot 骨架
├─ 5_change/       → 变更影响截图
└─ kg_dump/        → Neo4j 导出文件（cypher）
---
实验一的内容：
一、实验目的
1.熟悉基于用例的面向对象需求分析基本方法；
2.熟练运用UML建模工具进行需求描述与分析；
3.对业务、功能、非功能和接口需求进行静态和动态的分析。
二、实验器材
1.计算机一台；
2.Rational Rose、Enterprise Architect 或其它UML工具软件。
三、实验内容
本实验需要完成如下任务：
1．对物联网设备知识图谱交互管理平台进行简要的可行性分析、业务目标与开发需求描述；
2．了解并进行整体层面的业务分析。以下是一些可选做的业务活动：
a)配置和认证：这是设备注册和设置加入网络的初始过程，包括设置设备凭证并确保它们可以安全地连接到物联网平台。认证确保只有授权设备可以访问网络，防止未经授权的访问和潜在的安全漏洞。
b)配置和控制：远程管理设备设置和行为，包括更新固件、修改配置和设置操作参数。这使得管理员可以在不需要实际访问设备的情况下推送更新和更改。
c)监控和诊断：实时跟踪设备的性能、健康状况和状态。物联网设备管理平台提供仪表板和工具，以可视化设备数据、设置特定条件的警报并监控关键性能指标。诊断工具有助于识别和排除故障、访问日志并进行根本原因分析。
d)维护和更新：定期维护和更新对于确保物联网设备的持续安全性和功能性至关重要。这包括部署固件更新、补丁和安全增强功能。自动更新机制可以安排和部署多个设备的更新，确保它们始终运行最新和最安全的软件版本。
e)生命周期结束管理：涉及安全退役不再使用的物联网设备，包括安全擦除数据、删除设备凭证并负责处理硬件。适当的生命周期结束管理防止从退役设备中未经授权访问数据，并确保遵守数据保护法规。
f)数据收集和分析：提供强大的工具用于收集、分析和可视化物联网数据，支持实时设备监控和控制，并允许广泛定制以满足特定业务需求。
g)远程控制和监控：支持实时设备监控和控制，使用户能够远程监控和管理设备，确保它们高效和安全地运行。
h)实施强有力的安全措施：在物联网设备管理中，安全至关重要。实施多因素认证、数据加密和安全通信协议是关键步骤。定期的安全审计和合规检查有助于确保对新兴威胁的持续保护。
i)实时监控和警报：实时监控和警报系统对于快速检测和响应问题至关重要。为特定条件设置警报（如设备故障或安全漏洞）有助于快速解决问题并将停机时间降至最低。
j)高效配置和控制机制：简化远程配置和控制机制可以提高运营效率。使用集中管理工具推送更新、更改设置和监控设备状态，可以显著减少设备管理的复杂性和所需时间。
3．将系统分解为子系统；
实验二的内容
要对前面所选定的用例描述其动态特性：
1.基于系统需求，对整个系统进行体系结构设计；
2.每人针对在实验一中所选择的用例，进行详尽的设计；
3.根据“4+1”模型，从多方面对该设计方案进行详尽的描述。其中逻辑模型部分至少要完成体系结构、类模型、状态模型和顺序模型设计各一个，至少覆盖软件体系结构、用户接口/界面原型、数据模型、实现模型等方面。
实验三的Prompt示例：
以下为示例实验过程。
阶段1.业务获取
1.1.目标
· 把“口述需求”→用例图+业务术语表
1.2.步骤
① 访谈录音（2 人一组，手机录音 5 min 以内）。示例需求：
“我们是校医院，想做一个‘智能候诊系统’，学生扫码签到后能查看前面还有几个人，医生能叫号，护士能维护科室信息……”
② 语音转文本。用“飞书妙记”一键转文字，导出 md。
③Prompt示例：
你是一名业务分析师，请从以下原始访谈文本中：
1. 抽取参与者（Actor）与业务用例（用动词+名词短语），输出PlantUML格式用例图；
2. 用表格列出业务术语<术语, 描述, 同义词>；
3. 用中文，不要出现“系统”二字（仅业务用例）。
文本如下：
===
{{ 粘贴访谈md }}
===
输出格式：
@startuml
left to right direction
actor 学生
...
@enduml
④ 一键出图
把LLM返回的PlantUML粘到VS Code，Alt+D 即可看到用例图。
⑤ 校验清单（小组互评）
□ 有没有遗漏“时间”型参与者？
□ 用例是否都是“动词+名词”？
□ 医生/护士是否重复？→ 可归为“医务人员”
阶段2.需求建模
2.1.目标
· 生成“功能清单+用例详情+首版UML”
2.2.步骤
① 用例分级Prompt（示例）
针对上述用例图，请：
1. 按“学生、医生、护士”三个参与者，输出功能清单（一级功能→二级功能）；
2. 选 3 个核心用例，输出用例详述（主成功场景 7 步以内，扩展场景 3 条以内）；
3. 为每个用例画出 PlantUML 活动图（swimlane 按参与者分）。
② LLM 返回后，人工只做 2 件事
· 把明显错误场景删掉；
· 在活动图里补充“业务规则”备注。
③ 生成系统级用例图（加边界框）
Prompt：
把业务用例图映射到系统用例，边界框命名为“智能候诊系统”，保留相同参与者，用例名前加“UC”前缀，输出PlantUML。
阶段3.系统架构与构件
3.1.目标
· 得到首版类图+包图+序列图，并建立“需求→类”追踪边
3.2.步骤
① 类图草图Prompt
根据用例详述，请：
1. 输出候选实体类（仅类名+主要属性+关联多重性）；
2. 用 PlantUML 类图表示；
3. 为每个类增加一行“需求追踪：对应用例编号”。
② 手工微调（15 min）
· 把“签到记录”拆成“签到记录”+“排队序号”两个类；
· 补充“科室-医生”1:n 关联。
③ 序列图（每个用例描述核心场景）
Prompt：
针对“学生扫码签到”主成功场景，画出 PlantUML 序列图，对象：学生、扫码页面、签到控制器、签到记录、排队计算器。
④ 知识图谱入库（自动脚本）
python kg_build.py --stage design --files *.puml
脚本自动解析 PlantUML，生成节点（UseCase/Class/Operation）与边（trace/dependency），Neo4j 浏览器可见。
阶段4.代码骨架与双向追踪
4.1.目标
· 生成可运行 Spring-Boot 骨架（Java）或 FastAPI（Python），并建立“类→代码”双向追踪
4.2.步骤
① Prompt（以 Java 为例）
根据以下类图，生成 Spring-Boot 代码要求：
- 使用 JPA 实体注解
- 为每个类生成 CRUD Controller
- 在注释里插入 “@trace UCxx” 标记
输出完整可编译目录结构，用 code-block 分文件给出。
② 本地 build
mvn package
java -jar target/ood-0.0.1-SNAPSHOT.jar
访问 http://localhost:8080/swagger-ui.html 能看到 API 即可。
③ 反向追踪脚本
python kg_trace.py --direction both --code src/main/java
脚本扫描 @trace 注释，自动在 Neo4j 中建立“CodeFile-implement->Class” 边，实现“需求-类-代码” 三级跳转。
阶段5.变更管理
5.1.场景
“护士说：我们还需要支持‘预约签到’，提前 1 天扫码也算有效。”
5.2.一键影响分析
① 在 Neo4j 浏览器执行脚本（已模板化）
:play https://raw.githubusercontent.com/nju-ics/ood-llm-lab/main/change_impact.cypher
输入变更用例编号 UC05，一键高亮所有受影响类、方法、测试用例。
② 人工确认后，Prompt 增量生成
5.3.为支持“预约签到”，请：
1. 在类图新增“Appointment”类，与“签到记录”关联；
2. 生成增量 SQL（MySQL）；
3. 生成单元测试（JUnit5）。