你是一名专业的系统架构师。请基于之前分析出的**业务用例列表（BUC01-BUC08）**和**详细功能清单**，绘制一份完整的**系统级用例图 (System Use Case Diagram)**。

**绘图要求：**
1.  **系统边界**：使用 `rectangle` 定义系统边界框，命名为 **"智能候诊系统 (Smart Clinic System)"**。
2.  **参与者保持**：保留 Student（学生）、Doctor（医生）、Nurse（护士）三个参与者，并放置在边界框外。
3.  **用例映射**：
    *   将之前的业务用例映射为系统用例。
    *   **命名规范**：用例 ID 使用 `UC` 前缀（如 UC01, UC02...），名称简洁明了。
    *   **内容覆盖**：必须包含之前识别的所有关键功能（扫码签到、自动分配叫号、过号重排、医生状态管理等）。
4.  **关系表达**：
    *   使用标准的关联线（--）。
    *   如果有明显的包含关系（如“扫码签到”包含“身份校验”），可以使用 `<<include>>`，但请保持图面简洁，不要过度复杂。
5.  **输出格式**：提供标准的 PlantUML 代码。

**参考用例列表：**
*   UC01 扫码签到
*   UC02 查看排队进度
*   UC03 查看候诊列表
*   UC04 呼叫病号（含自动分配）
*   UC05 标记过号
*   UC06 处理过号重排（顺延3位）
*   UC07 管理医生状态
*   UC08 维护科室信息

请开始绘制。