你是一名精通 UML 建模的系统架构师。请根据以下提供的三个核心用例详述（MSS 和 Extensions），绘制出对应的 **PlantUML 活动图（Activity Diagram）**。

**绘图要求：**
1.  **必须使用泳道（Swimlanes/Partitions）**：
    *   左侧泳道为 **参与者**（Student/Doctor/Nurse）。
    *   右侧泳道为 **系统**（System）。
2.  **逻辑完整**：
    *   主成功场景（MSS）作为主流程。
    *   扩展场景（Extensions）必须通过 `if-else` 判断菱形来表示分支。
3.  **细节标注**：
    *   对于关键的业务规则（如“顺延3位”、“自动分配”），请在图中用 `note right` 语法添加备注。
4.  **输出格式**：请提供 3 段独立的 PlantUML 代码块。

**输入数据（用例详述）：**

---

### 用例 1：扫码签到 (BUC01)
*   **参与者：** 学生
*   **流程要点：**
    1. 学生扫码 -> 系统校验位置/身份。
    2. 系统校验挂号记录 -> 若无记录，提示去挂号（结束）；若有记录，继续。
    3. 系统校验是否重复 -> 若重复，提示勿重复（结束）；若不重复，继续。
    4. 系统更新状态为“候诊中” -> 加入队列尾部 -> 返回成功及排队信息。

### 用例 2：呼叫病号 (BUC04)
*   **参与者：** 医生
*   **流程要点：**
    1. 医生点击叫号 -> 系统检查队列是否为空。
    2. 若为空 -> 提示无病人（结束）。
    3. 若不为空 -> 系统锁定队首 -> **执行自动分配逻辑**（绑定医生） -> 更新状态。
    4. 系统触发大屏和手机通知 -> 加载病历 -> 医生端刷新。

### 用例 3：处理过号重排 (BUC06)
*   **参与者：** 护士
*   **流程要点：**
    1. 护士查询学生 -> 系统显示状态。
    2. 护士点击重排 -> 系统校验状态是否为“过号”。
    3. 若非过号 -> 提示错误（结束）。
    4. 若为过号 -> 系统校验是否超过重排次数限制（如3次）。
    5. 若超限 -> 提示重新挂号（结束）。
    6. 若符合条件 -> **计算插入位置 (Target = 当前N + 3)** -> 执行插入操作 -> 提示成功。

---

请根据以上信息，生成三个高质量的 PlantUML 活动图代码。